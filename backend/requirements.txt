# Web framework
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9

# Auth & Security
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
supabase>=2.28.0
websockets>=15.0      # >=15 required by yfinance and supabase realtime

# Database / async
asyncpg==0.29.0

# Redis
redis==5.0.4
hiredis==2.3.2

# Data
pandas>=2.2
numpy>=1.26
yfinance>=0.2.40
pandas-ta==0.4.71b0

# ML
scikit-learn==1.5.0
xgboost==2.0.3
torch>=2.4.0          # Apple Silicon MPS supported (2.4+ required by transformers)
arch>=6.3             # GARCH(1,1) volatility forecasting
transformers>=4.44,<5.0  # FinBERT (ProsusAI/finbert) sentiment

# Broker integrations
kiteconnect==4.2.0     # Zerodha
smartapi-python==1.3.0 # Angel One

# Optional (large build — install separately):
# prophet               — pip install prophet  (requires cmdstanpy; ~5min build)

# HTTP clients
httpx==0.27.0
aiohttp==3.9.5

# Task queue
celery[redis]==5.4.0

# Utils
python-dotenv==1.0.1
pydantic>=2.11.7
pydantic-settings>=2.3.0
cryptography==42.0.8
loguru==0.7.2

# Dev / Test
pytest==8.2.1
pytest-asyncio==0.23.7
